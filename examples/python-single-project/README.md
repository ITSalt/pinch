# python-single-project

Python equivalent of `node-single-project`. Three prompts, one
project, default invariants.

## Run

```bash
pip install itsalt-pinch
python main.py
```

First task fires after a ~15–30 s spawn delay; subsequent tasks wait
at least 15 s after the previous one.
